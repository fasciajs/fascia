# A version must be one day old before this workspace resolves it. A compromised release is
# normally found and unpublished inside that window, and an install that waits never sees it.
# npm 12 sets this default. The line states the value for an older npm as well.
min-release-age=1440
