/**
 * OpenAPI 3.1, as its own maintainers declare it.
 *
 * `openapi-types` is the published declaration and this file names it rather than restating it. The
 * ATD package learned that lesson the hard way: a type written from a specification's prose made
 * `properties` optional where arri requires it, and the document that came out accepted every value.
 */
export type { OpenAPIV3_1 as V31 } from 'openapi-types'
